[project]
name = "co-pymol"
version = "0.3.0"
description = "PyMOL plugin that exposes PyMOL as tools for MCP clients"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "mcp~=1.27.1",
    "anyio>=4.0",
    "numpy>=1.24",
    "gemmi>=0.6",
    # PyMOL's Intel macOS bundle lacks Python headers, so cryptography must use
    # the last release line that supplies a compatible prebuilt wheel.
    "cryptography<46; sys_platform == 'darwin' and platform_machine == 'x86_64'",
]

[project.optional-dependencies]
dev = [
    "pre-commit",
    "pytest>=8.0",
    "ruff",
]

[project.scripts]
co-pymol = "co_pymol.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
quote-style = "double"

[tool.hatch.build.targets.wheel]
packages = ["src/co_pymol"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
